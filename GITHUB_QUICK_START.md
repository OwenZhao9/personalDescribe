# GitHub 部署快速开始

## 🚀 最简单的方式：Vercel（推荐）

### 3 步完成部署

1. **访问 [vercel.com](https://vercel.com)**，用 GitHub 账号登录

2. **点击 "New Project"**，选择您的 GitHub 仓库

3. **点击 Deploy**（配置已自动识别）

✅ 完成！您的网站会在几分钟内上线

**访问地址**：`https://your-project.vercel.app`

---

## 📄 GitHub Pages 部署

### 步骤

1. **推送代码到 GitHub**
   ```bash
   git add .
   git commit -m "准备部署"
   git push origin main
   ```

2. **启用 GitHub Pages**
   - 进入仓库 Settings → Pages
   - Source 选择：**GitHub Actions**
   - 保存

3. **修改 vite.config.ts**（如果需要自定义域名，跳过此步）
   
   如果仓库名是 `zhaoning-resume`，需要设置 base：
   ```typescript
   export default defineConfig({
     base: '/zhaoning-resume/', // 改为您的仓库名
     // ... 其他配置
   })
   ```

4. **等待 GitHub Actions 自动部署**

**访问地址**：
- 如果仓库名是 `zhaoning-resume`：`https://your-username.github.io/zhaoning-resume/`
- 如果配置了自定义域名：`https://your-domain.com`

---

## ☁️ Cloudflare Pages 部署

### 步骤

1. **访问 [pages.cloudflare.com](https://pages.cloudflare.com)**，用 GitHub 登录

2. **连接仓库**，选择您的项目

3. **配置构建**：
   - Build command: `pnpm build`
   - Build output directory: `dist/public`

4. **点击 Deploy**

**访问地址**：`https://your-project.pages.dev`

---

## 🌐 Netlify 部署

### 步骤

1. **访问 [netlify.com](https://www.netlify.com)**，用 GitHub 登录

2. **点击 "Add new site" → "Import an existing project"**

3. **选择 GitHub 仓库**

4. **配置**（通常会自动识别）：
   - Build command: `pnpm build`
   - Publish directory: `dist/public`

5. **点击 Deploy**

**访问地址**：`https://your-project.netlify.app`

---

## ⚙️ 自定义域名

所有平台都支持自定义域名：

1. 在平台设置中添加您的域名
2. 按照提示配置 DNS 记录
3. 等待 SSL 证书自动配置（通常几分钟）

---

## 🔄 自动更新

所有平台都支持自动部署：
- 每次推送到 `main` 分支会自动重新部署
- 无需手动操作

---

## 📊 方案对比

| 平台 | 免费 | 国内速度 | 推荐度 |
|------|------|---------|--------|
| **Vercel** | ✅ | ⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ |
| **Cloudflare Pages** | ✅ | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ |
| **GitHub Pages** | ✅ | ⭐⭐⭐ | ⭐⭐⭐⭐ |
| **Netlify** | ✅ | ⭐⭐⭐ | ⭐⭐⭐⭐ |

**最佳推荐**：**Vercel** 或 **Cloudflare Pages**

---

## ❓ 常见问题

**Q: 部署后页面空白？**
- 检查控制台错误
- 确认构建输出目录正确：`dist/public`

**Q: 路由不工作？**
- Vercel/Netlify/Cloudflare 已自动配置
- GitHub Pages 需要 404.html（已包含）

**Q: 如何更新网站？**
- 直接 push 代码到 GitHub，平台会自动部署

**Q: 可以同时部署到多个平台吗？**
- 可以！互不冲突

---

## 🎯 推荐流程

1. **首选**：使用 **Vercel**（最简单，3分钟完成）
2. **备选**：使用 **Cloudflare Pages**（国内访问最快）
3. **学习**：使用 **GitHub Pages**（了解 GitHub Actions）

选择任一方案，按照对应步骤操作即可！

